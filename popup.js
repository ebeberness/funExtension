console.log("Drink Water!");


function listen(eventType, selector, callback) {
    document.querySelector(selector).addEventListener(eventType, callback);
  }

// function takes in an event type, i.e. click, moudsedown, etc.
// takes in a selector, css selector, ID of different elements
// Call back function
  
  
  
  